# Copy this file to .env.local and fill with your Firebase project values.
# Do NOT commit .env.local — it should remain private.

# Firebase frontend config (use values from your Firebase console -> Project settings)
NEXT_PUBLIC_FIREBASE_API_KEY=YOUR_FIREBASE_API_KEY
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com

# Backend URL used by the frontend to call server endpoints
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000
